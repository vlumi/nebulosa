# nginx server block for nebulosa.misaki.fi.
#
# Start HTTP-only so certbot can complete the ACME challenge, then
#   sudo certbot --nginx -d nebulosa.misaki.fi
# adds the 443 block and the HTTP->HTTPS redirect.
#
# Layout under the web root (WEBROOT in deploy.sh, default /var/www/nebulosa), owned by
# the deploying user:
#   current -> releases/<sha>   flipped by deploy.sh
#   data/tles.json              written daily by the host's cron job

server {
    listen 80;
    listen [::]:80;
    server_name nebulosa.misaki.fi;

    root /var/www/nebulosa/current;
    index index.html;

    # Single-page app: every path falls back to index.html.
    location / {
        try_files $uri /index.html;
        add_header Cache-Control "no-cache";
    }

    # Vite fingerprints everything under /assets/, so cache hard.
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Orbital elements live outside the releases so deploys never touch them.
    location /data/ {
        alias /var/www/nebulosa/data/;
        add_header Cache-Control "no-cache";
    }

    location ~ /\. {
        deny all;
    }
}
