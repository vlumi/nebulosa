# The registry audit endpoint intermittently hangs for npm's full 5-minute fetch timeout,
# turning a 7-second install into a 5-minute one. Vulnerability alerts come from GitHub instead.
audit=false
fund=false
